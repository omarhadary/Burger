<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<div class="row top-row">
		<div class="col-sm-offset-4 col-sm-4">
			<img alt="Delicious" src="/assets/img/burger.png">
			<h1>Eat-Da-Burger!</h1>
		</div>
	</div>
	<div class="row middle-row">
		<div class="col-sm-2"></div>
        <div class="col-sm-4">
            
                {{#each burgers}}
                    {{#unless this.devoured}}
                        <div>
                            {{this.id}}.
                            {{this.burger_name}}
                            <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                                <input type="hidden" name="devoured" value="true">
                                <button type="submit">Devour it!</button>
                            </form>
                        </div>
                    {{/unless}}
                {{/each}}
            
        </div>
		<div class="col-sm-4">
            
                {{#each burgers}}
                    {{#if this.devoured}}
                        <div>
                            {{this.id}}.
                            {{this.burger_name}}
                            <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                                <input type="hidden" name="devoured" value="false">
                            </form>
                        </div>
                    {{/if}}
                {{/each}}
            
        </div>
        <div class="col-sm-2"></div>
	</div>
	<div class="row bottom-row">
		<div class="col-sm-offset-4 col-sm-4 centered">
			<form id="submit-form" name="submit-form" action="burgers/add" method="POST">
				<input id="submit-input" type="text" name="burger_name"><br>
				<input id="add-burger" type="submit" value="Submit"><br>
				<label for="submit-input">Enter a burger you want to eat. Click the "Submit" button.</label><br>
			</form>
		</div>
	</div>
</body>
</html>